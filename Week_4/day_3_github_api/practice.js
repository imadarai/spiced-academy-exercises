
var a = 1; //Var has scope availability.  It's availabele only wihtin the scop, e.g. function;
let b = 2; //Let has block scope.  So it's only available within it's block.  e.g. if statement
const c = 3;
// var name = `name`; //backticks for normal strings AND


let name = `imad`;
let lastName = `arain`;
let fullName = `${name} ${lastName}`;

console.log(fullName);
